[build]
    publish = "public"
    command = "npx tinacms build && npm run build"

[context.production.environment]
    HUGO_VERSION = "0.79.0"
    HUGO_ENV = "production"
    HUGO_ENABLEGITINFO = "true"

[build.environment]
    TZ = "Asia/Kolkata"
